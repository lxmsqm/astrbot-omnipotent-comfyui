# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/

# IDE / Editor
.codebuddy/
.idea/
.vscode/
*.swp
*.swo
*~

# Local runtime config (contains user machine paths)
plugin_config.json

# User data directory (gitignored — each user's personal config)
data/user/
# v4.3.0 数据分离迁移后留下的旧数据备份（真实数据在 AstrBot/data/comfyui_allinone_data/）
data/user_migrated_backup/
data/anima_tools_migrated_backup/

# Local machine paths / OS
previews/
.DS_Store
Thumbs.db

# Sensitive content (optional - uncomment if not needed)
# data/pose_action/Sex positions.json

# Handover document (internal only)
本次交接文档.md
交接文档.md

# Backup files
*.bak

# Cache (symlinked to E: drive)
data/cache/

# Local user data (runtime)
data/user/

# K2 engine toolchain test scripts (dev only)
k2gen/test/

# Dev/test artifacts (keep repo clean)
check_webui_local.js
k2gen.tar.gz
