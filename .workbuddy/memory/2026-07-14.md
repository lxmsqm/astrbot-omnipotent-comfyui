# 2026-07-14

## 修复: "清除所有指定" 不清理禁用节点
- **webui.html** `clearAllNodeRoles()`:
  - `hasAny` 守卫条件补上 `|| disabledNodes.length > 0`
  - 清理逻辑补上 `disabledNodes = [];`
- 之前点击按钮无法清除禁用节点的选择和标签显示

## 修复: 禁用节点跨工作流残留
- **main.py** `_webui_save_workflow_params`: 将 `__disabled_nodes__` 从根级别存储改为 per-workflow 存储
  - 加入 per-workflow 键列表（line 1743）
  - 从 `preserved_keys` 移除（line 1749）
- **main.py** `_webui_get_workflow_params_config`: 读取路径改为 `wf_config` 优先，root fallback 兼容旧数据（line 1842）
- **main.py** `_apply_workflow_config`: 应用禁用节点时也从 `wf_cfg` 读取，保留 root fallback（line 2783）
- 现在禁用节点配置随工作流隔离，删除工作流时自动清理

## 优化: 节点选择图标 emoji → SVG
- **webui.html** 节点按钮（line 5265-5273）：替换 emoji（🎯🚫🖼️📐📝⛔✅）为统一 SVG 图标
  - 正面提示词: `#icon-check` ✓
  - 负面提示词: `#icon-prohibited` ⊘
  - 载入图片1/2/3: `#icon-image` + `.btn-badge` 数字角标
  - 分辨率: `#icon-ruler` ▤
  - 扩写后文本: `#icon-file-text` 📄
  - 禁用/启用: `#icon-prohibited`（统一用 `active-danger` 区分状态）
- 新增 CSS: `.node-tag-btn svg{width:14px;height:14px}`, `.btn-badge{font-size:8px;font-weight:700}`

## 优化: 禁用节点卡片样式提亮
- **webui.html** `.node-card.node-disabled`:
  - opacity: 0.45 → 0.7（更亮，文字可读）
  - filter:grayscale: 0.6 → 0.35（保留更多色彩）
  - border: 红色 danger → 深灰 `var(--text-muted)`（黑边效果）
  - ::before 顶条: 红色 danger → 深灰 `var(--text-muted)`

## 修复: icon-file-text 不存在导致空白框
- SVG sprite 中没有 `#icon-file-text` 定义，3 处引用改为 `#icon-document`
- 涉及：节点按钮、section header、assigned tags

## 优化: 节点帮助栏补全颜色提示
- 新增 `.node-help-dot.exp`（紫色 accent）表示扩写文本
- 新增 `.node-help-dot.dis`（灰色 text-muted）表示禁用
- HTML 帮助栏补上「文本」「禁用」两项

## 优化: 魔导书主/子分类视觉区分
- `.source-group-header`: 加 3px primary 左边框 + 淡紫底 + 13px 粗体白字
- 子分类保持缩进 + 11px 中性色

## 补充: 5个子分类各约100条通用数据
生成脚本 `scripts/generate_grimoire_data.py`
| 子分类 | 原数量 | 现数量 |
|--------|-------|-------|
| lighting/lighting.json | 10 | 114 |
| pose_action/normal_posture.json | 10 | 111 |
| scene/environment.json | 10 | 112 |
| shot/framing.json | 11 | 112 |
| anima/lora角色.json | 6 | 114 |

## 修复: FAB 菜单图标状态不一致
- `closeFabMenu` 强制 inline `opacity:0; pointer-events:none` 防御残留
- `toggleFabMenu` 打开时**清空** inline 样式（让 `.fab-menu.open` CSS class 生效）
- `showGrimoire` 函数开头防御性调用 `closeFabMenu`（处理底部导航/外部入口）

## 修复: 手机节点卡底部按钮被切
- `@media(max-width:480px)` 里 `.node-grid` 改用 `max-height:50vh` 替代固定 360px
- `.node-grid::after` 高度 50px → 120px，滚动到最底完整露出最后一行按钮

## 新增: LLM 魔导书 CRUD 工具
- **`_grimoire_sources()`** 动态扫描 data/ 目录
- **`_grimoire_find_source_path()`** 中文/文件名模糊匹配
- **`_grimoire_read/write()`** 读写封装
- **3 个新 LLM 工具**: add / delete / edit
- **`comfyui_get_prompt(message_id)`** 查图片提示词
- **更新** 4 个已有工具的 name_map → 动态源兜底
