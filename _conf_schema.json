{
  "webui": {
    "title": "管理面板",
    "description": "点击打开 WebUI 管理面板：http://127.0.0.1:8898",
    "type": "string",
    "default": "http://127.0.0.1:8898"
  },
  "webui_lan": {
    "title": "局域网访问",
    "description": "开启后可通过局域网 IP 访问 WebUI（无需重启，即时生效）",
    "type": "bool",
    "default": false
  },
  "webui_ipv6": {
    "title": "IPv6 访问",
    "description": "开启后可通过 IPv6 地址访问 WebUI（无需重启，即时生效）",
    "type": "bool",
    "default": false
  },
  "prompt_log_days": {
    "title": "提示词记录天数",
    "description": "引用图片查询提示词的记录保留天数，默认 3 天，填 0 关闭",
    "type": "int",
    "default": 3
  },
  "show_prompt_on_image": {
    "title": "图片附带提示词",
    "description": "每次生成的图片消息末尾自动附上最终提示词",
    "type": "bool",
    "default": false
  },
  "send_platform": {
    "title": "结果发送平台（命令回复）",
    "description": "QQ/飞书命令触发的生成结果发到哪：auto=自动识别来源平台（推荐）；qq=固定发QQ；feishu=固定发飞书；both=两边都发",
    "type": "string",
    "default": "auto"
  },
  "target_platform": {
    "title": "生成后发送到（平台）",
    "description": "WebUI 点「生成」后主动推送的平台：qq=QQ；feishu=飞书",
    "type": "string",
    "default": "qq"
  },
  "target_id": {
    "title": "生成后发送到（ID）",
    "description": "目标 ID：QQ 填 QQ号/QQ群号；飞书填 open_id(ou_xxx) 或群 chat_id(oc_xxx)。留空则不主动发送",
    "type": "string",
    "default": ""
  },
  "target_group": {
    "title": "目标是群聊",
    "description": "勾选后按群消息发送（QQ群 / 飞书群）",
    "type": "bool",
    "default": false
  },
  "anima_spec_enhance": {
    "title": "Anima 随机规范增强",
    "description": "开启后，Anima 模式随机图会自动按官方规范组织提示词：固定开头 masterpiece/best quality/score_7/safe、补主体数 1girl solo、并在末尾追加 1~2 句自然语言描述（tag+自然语言混合写法）。仅 anima 模式生效；K2 模式（中文成句引擎）不受影响。注意：score_7 适用于 Base 版模型，Aesthetic 版请关闭本项",
    "type": "bool",
    "default": true
  },
  "anima_artist_limit": {
    "title": "画师库加载上限",
    "description": "从 animadex API 加载的画师数量。默认 0 = 全量（有多少拉多少，约 1.5 万条，按 liked 热度排序，只收有图的）。填正数则只加载前 N 个热门画师（如 500，可加快首次加载）。画师标签自动规范成 @名字 写入提示词；首次全量加载约需数分钟，之后直接读本地缓存",
    "type": "int",
    "default": 0
  },
  "anima_character_limit": {
    "title": "角色库加载上限",
    "description": "从 animadex API 加载的角色数量。默认 0 = 全量（有多少拉多少，约 3.6 万条，按 liked 热度排序，只收有图的）。填正数则只加载前 N 个热门角色。角色数据含规范触发词（角色名+系列）、外观标签数组、可用缩略图；首次全量加载约需 10-20 分钟，之后直接读本地缓存",
    "type": "int",
    "default": 0
  },
  "anima_nsfw": {
    "title": "Anima 随机允许 NSFW",
    "description": "关闭时（默认），随机抽取会跳过「明确色情」标签（裸体/性行为/性器官/体液/BDSM 等），从同分类继续抽下一个，保证每个分类仍有产出。宽松档：保留内衣/泳装/比基尼/透视/乳沟等擦边内容。开启后不做任何过滤。K2 模式使用其独立的 NSFW 开关",
    "type": "bool",
    "default": false
  }
}
