# ComfyUI 全能插件 — 使用教程

AstrBot 插件，连接本地/云端 ComfyUI，实现**文生图、图生图、图生视频、图片编辑**。自带 WebUI 可视化面板（赛博魔法主题）、魔导书提示词库。

**v3.3.6 新增：**
- ⛔ **节点禁用/绕过** — WebUI 中可禁用 Lora、扩写等节点，物理删除+重连，安全绕过
- 🎯 **主模型切换修复** — 节点参数修改后真正生效，不再被 `allowed_nodes` 限制跳过
- 🛡️ **过期模型值保护** — 自动跳过已失效的模型/Lora 保存值，避免 ComfyUI 校验报错
- 🧹 **路径自动清理** — 保存的模型名含路径前缀时自动提取纯文件名

---

## 📖 目录

1. [快速开始](#1-快速开始)
2. [命令详解](#2-命令详解)
   - [2.1 🎨 `/画` — 文生图](#21-%EF%B8%8F-画--文生图)
   - [2.2 🖼️ `/图生图` — 图生图](#22-️-图生图--图生图)
   - [2.3 🎬 `/图生视频` — 图生视频](#23--图生视频--图生视频)
   - [2.4 ✏️ `/编辑` — 图片编辑](#24-️-编辑--图片编辑)
   - [2.5 📂 `/工作流` — 工作流管理](#25--工作流--工作流管理)
   - [2.6 📐 `/比例` — 图片比例](#26--比例--图片比例)
   - [2.7 🎚️ `/分辨率` — 质量等级](#27-️-分辨率--质量等级)
   - [2.8 🚀 `/执行` — 执行工作流](#28--执行--执行工作流)
   - [2.9 📊 `/队列` — 查看队列](#29--队列--查看队列)
   - [2.10 ⏹️ `/停止` — 停止生成](#210-️-停止--停止生成)
   - [2.11 🎲 `/随机图` — 随机图片](#211--随机图--随机图片)
   - [2.12 ↩️ `/撤回` — 撤回消息](#212-️-撤回--撤回消息)
   - [2.13 🔍 `/提示词` — 查询提示词](#213--提示词--查询提示词)
   - [2.14 ❓ `/帮助` — 帮助](#214--帮助--帮助)
   - [2.15 🔢 数字回复 — 选择确认](#215--数字回复--选择确认)
3. [WebUI 管理面板](#3-webui-管理面板)
4. [工作流配置](#4-工作流配置)
5. [魔导书系统](#5-魔导书系统)
   - [5.1 什么是魔导书？](#51-什么是魔导书)
   - [5.2 数据来源](#52-数据来源)
   - [5.3 在 WebUI 中使用](#53-在-webui-中使用)
   - [5.4 固定标签（Pin）系统](#54-固定标签pin系统)
   - [5.5 随机池（Random Pool）](#55-随机池random-pool)
   - [5.6 LLM 自动搜索](#56-llm-自动搜索)
   - [5.7 自定义扩展数据](#57-自定义扩展数据)
6. [LLM 工具调用](#6-llm-工具调用)
7. [配置项说明](#7-配置项说明)
8. [常见问题](#8-常见问题)

---

## 1. 快速开始

### 1.1 前置条件

| 条件 | 说明 |
|------|------|
| ComfyUI | 已部署并运行，默认地址 `127.0.0.1:8188` |
| AstrBot | 已安装并运行 |
| 工作流文件 | 准备好 **API 格式** 的 `.json` 工作流文件 |

> **什么是 API 格式的工作流？** 在 ComfyUI 中加载工作流 → 右上角「Save (API Format)」导出的 JSON 文件。普通工作流 JSON 不能被插件直接使用。

### 1.2 安装

将整个插件文件夹放入 AstrBot 的 `data/plugins/` 目录，重启 AstrBot 即可自动加载。

启动后日志会输出 WebUI 地址：

```
[ComfyUI] WebUI: http://127.0.0.1:8898 | 工作流目录: E:\xxx\workflows | 上传模式: local
```

### 1.3 首次配置四步走

```
Step 1 ─ 打开 WebUI → http://127.0.0.1:8898
Step 2 ─ 点击右上角 ⚙ 设置 → 配置工作流目录、输出目录、ComfyUI 地址
Step 3 ─ 在工作流管理器中为每个工作流设置「分类」标签
Step 4 ─ 在节点配置面板中标记正面提示词节点、分辨率节点等
```

完成后即可在 QQ 群内发送：

```
/画 一个白发Miku
```

### 1.4 目录结构速览

```
工作流目录/
├── Anima动漫画图.json       ← API 格式工作流
├── FLUX漫转真人.json
├── 文生视频.json
└── 原json/                  ← 非 API 格式备份（插件不会读取）
```

---

## 2. 命令详解

所有命令均在 QQ 群内以 `/` 开头发送。

### 2.1 🎨 `/画` — 文生图

```
/画 一个白发Miku
/画 16:9 一个白发Miku
```

- **第一个参数可选**：图片比例（如 `9:16`、`1:1`、`4:3`、`16:9` 等）
- **剩余部分**：提示词
- 不加比例时使用当前默认比例

### 2.2 🖼️ `/图生图` — 图生图

```
/图生图 把背景改成红色
/图生图 0.7 把背景改成红色
```

**如何提供参考图（二选一）：**

| 方式 | 操作 |
|------|------|
| 引用图片 | 回复/引用一条带图片的消息，然后发送命令 |
| @用户头像 | `@用户` + 发送命令（无引用图片时生效） |

**降噪值**（可选第一个参数）：`0.1` ~ `0.8`，越低变化越小，默认由工作流决定。

### 2.3 🎬 `/图生视频` — 图生视频

```
/图生视频
```

- 需引用图片或 @用户获取头像
- 当前工作流名称需含 `视频`、`wan`、`ltx`、`animate`、`video` 等关键词（插件据此判断超时时间）
- 视频生成时间较长，超时时间 900 秒

### 2.4 ✏️ `/编辑` — 图片编辑

```
/编辑 把这个猫换成狗
```

- 支持引用图片、@用户、上传图片（最多 3 张参考图）
- 图片按顺序传入工作流的 LoadImage 节点
- 无参考图时会提示

### 2.5 📂 `/工作流` — 工作流管理

```
/工作流            → 显示所有工作流（按分类分组），✅ 标记当前
/工作流 3          → 切换到编号 3 的工作流
/工作流 anime      → 按关键词搜索并切换
/切换 anime        → 同上，快捷方式
```

选择后 **10 秒内**回复数字可切换。

### 2.6 📐 `/比例` — 图片比例

```
/比例              → 显示所有可用比例
/比例 3            → 切换到编号 3
/比例 16:9         → 按名称切换
```

可用比例列表：`1:1` `3:4` `4:3` `9:16` `16:9` `2:3` `3:2`

### 2.7 🎚️ `/分辨率` — 质量等级

```
/分辨率            → 显示所有质量等级
/分辨率 2          → 切换到编号 2
/分辨率 1080p      → 按名称切换
```

| 等级 | 像素数 | 说明 |
|------|--------|------|
| 480p | ~39.9 万 | 快速出图，SD 质量 |
| 720p | ~92.2 万 | 标清（默认） |
| 1080p | ~207 万 | 高清 |
| 2K | ~369 万 | 超清 |
| 4K | ~829 万 | 原画，最慢 |

> **质量与比例的关系**：质量等级决定图片的总像素数，比例决定宽高比。插件自动计算实际的 `width` 和 `height`。例如 720p + 9:16 = 720×1280。

### 2.8 🚀 `/执行` — 执行工作流

```
/执行 提示词
```

- 执行当前工作流，**不限制分类**
- 适用于未设置分类的工作流或需要临时覆盖分类的场景

### 2.9 📊 `/队列` — 查看队列

```
/队列
```

显示 ComfyUI 当前队列状态：运行中任务数 + 等待中任务数。

### 2.10 ⏹️ `/停止` — 停止生成

```
/停止
```

停止**当前用户**提交的正在生成的任务，不影响其他用户。

### 2.11 🎲 `/随机图` — 随机图片

```
/随机图          → 随机生成 1 张
/随机图 5        → 随机生成 5 张（最多 10 张）
```

- 从魔导书随机池中抽取标签，合并固定标签后生成随机图片
- 需先在魔导书 WebUI 中将数据源加入「随机池」

### 2.12 ↩️ `/撤回` — 撤回消息

```
/撤回
```

撤回本插件在该会话中最近发送的一张图片/视频。

> ⚠️ 撤回依赖 OneBot API 返回的 `message_id`，仅 QQ 平台支持。Telegram/Discord 等平台不可用。

### 2.13 🔍 `/提示词` — 查询提示词

```
@bot 提示词     ← 需引用一张 bot 发过的图片
```

返回该图片生成时使用的提示词。支持扩写后文本节点配置的工作流，会返回扩写后的完整提示词（而非用户原始输入）。

> 提示词记录保存在 `data/prompt_log.json`，默认保留 3 天，可在管理面板或 AStrBot 配置中调整 `prompt_log_days`。

### 2.14 ❓ `/帮助` — 帮助

```
/帮助
```

显示当前工作流名称和所有可用命令。

### 2.14 🔢 数字回复 — 选择确认

发送 `/工作流`、`/比例`、`/分辨率` 等命令后，插件会列出选项列表，在 **10 秒内**直接回复数字即可选择。例如：

```
你: /工作流
机器人: 请选择工作流：
  1. Anima动漫画图 ✅
  2. FLUX漫转真
  3. 基础图生视频
你: 2          ← 10 秒内回复数字
机器人: 已切换到 FLUX漫转真
```

---

## 3. WebUI 管理面板

### 3.1 访问方式

| 场景 | 地址 |
|------|------|
| 本机访问 | `http://127.0.0.1:8898` |
| 局域网访问 | `http://你的局域网IP:8898`（需在设置中开启 LAN） |
| 云端访问 | `http://你的服务器公网IP:8898`（需开放防火墙 + 开启 LAN） |

> **浏览器推荐**：Firefox。该 WebUI 使用了大量 `backdrop-filter: blur()` 玻璃态效果，Edge/Chrome 可能因 GPU 黑名单导致卡顿，Firefox 全速运行。

### 3.2 界面分区

```
┌─────────────────────────────────────────────────┐
│  🪄 创作画室           [🌙/☀️] [⚙️]             │  ← 上行：品牌 + 主题切换
│  ● 已连接  队列: 0  [Anima动漫画图] 📶 🌐 ☁️ 🖥️ │  ← 下行：状态栏
├────────────┬────────────────────────────────────┤
│            │                                    │
│  工作流    │   提示词输入区域                    │
│  管理器    │   [正面提示词...]                   │
│            │   [负面提示词...]                   │
│  ────────  │                                    │
│  按分类    │   📐 参数面板                      │
│  查看工作流│   质量: 720p  比例: 9:16            │
│            │                                    │
│            │   📖 魔导书标签搜索                │
│            │                                    │
│            │   🌄 画廊                          │
│            │   [图片瀑布流]                     │
├────────────┴────────────────────────────────────┤
│  底部状态栏: 进度条 / 计时器                    │
└─────────────────────────────────────────────────┘
```

### 3.3 功能详解

#### 工作流管理器（左侧栏）

| 操作 | 说明 |
|------|------|
| 查看工作流 | 按分类（画/图生图/图生视频/编辑）分组显示 |
| 切换工作流 | 点击工作流名称即可切换 |
| 搜索工作流 | 顶部搜索框按关键词过滤 |
| 隐藏工作流 | 右键/菜单中设为隐藏，不在列表中显示 |
| 分类管理 | 为每个工作流设置分类标签 |
| 预览图 | 可上传预览图便于识别 |

#### 节点配置面板

在工作流管理器中点击「配置节点」进入，标记各节点的角色：

| 角色 | 按钮 | 说明 |
|------|------|------|
| 🎯 正面提示词 | `setPromptNode` | 插件往此节点写入用户输入的提示词 |
| 🚫 负面提示词 | `setNegativeNode` | 插件往此节点写入通用负面词 |
| 📐 分辨率 | `setResNode` | 插件覆盖此节点的宽高 |
| 📤 图片上传 1 | `setLoadImageNode0` | 图生图/编辑时传入第一张参考图 |
| 📤 图片上传 2 | `setLoadImageNode1` | 传入第二张参考图 |
| 📤 图片上传 3 | `setLoadImageNode2` | 传入第三张参考图 |
| 📝 扩写后文本 | `setExpandedTextNode` | 从此节点读取扩写后的提示词，替代原始输入存入提示词记录 |

> **核心原则**：插件**只会覆盖**你在 WebUI 中显式标记过的节点。未标记的节点保持工作流原有值不变。这意味着你可以精细控制哪些参数由插件接管。

#### 魔导书面板

点击正面提示词框上方的 📖 按钮打开底部面板，是一个完整的提示词标签管理系统：

- **搜索标签**：按子分类浏览（角色/画师/服装/场景/光影/姿势/镜头），支持关键词搜索和IP分类筛选
- **复制标签**：点击条目的 📋 按钮，tags 自动复制到剪贴板
- **固定标签（📌）**：将常用条目固定，标签会自动合并到每次生图的 prompt 中（有冲突检测）
- **随机池（🎲）**：将子分类加入随机池，`/随机图` 命令会从中各随机抽取一条
- **数据管理**：支持新增/编辑/删除条目，批量导入/导出，管理数据源和分类目录
- **编辑模式**：点击「编辑」进入，可批量选中删除

#### 画廊

纵向滚动浏览所有已生成的图片，支持：

- 点击图片放大预览（灯箱效果）
- 下载原图
- 单张/批量删除
- 撤回（同 `/撤回` 命令）

#### 设置面板（右上角 ⚙）

| 配置项 | 说明 |
|--------|------|
| 工作流目录 | 存放工作流 JSON 文件的目录 |
| 输出目录 | 生成图片的保存目录 |
| ComfyUI 输入目录 | 上传图片写入的目录（默认 `输出目录/upload/`） |
| ComfyUI 地址 | ComfyUI 服务地址（默认 `127.0.0.1:8188`） |
| 上传模式 | `本地模式`：图片上传到 ComfyUI input 目录；`远程模式`：上传到插件 upload 目录 |
| 局域网访问 | 开启后可通过 `http://你的IP:8898` 局域网访问 |
| IPv6 访问 | 开启后可通过 IPv6 访问 |
| 图片消息附带提示词 | 开启后每次生成的图片消息末尾自动附上最终提示词（含扩写后文本） |

#### 顶部栏状态图标

| 图标 | 说明 |
|------|------|
| ● 绿色呼吸脉冲 | ComfyUI 连接正常（脉冲动画） |
| ● 红色常亮 | ComfyUI 连接断开 |
| ☁️ 滑块 | 界面透明度（30%~100%） |

#### WebUI 视觉特效

| 效果 | 说明 |
|------|------|
| 🌌 **极光背景** | 4层径向渐变 + 5个浮动光球 + 网格 + 星光 + 噪点纹理 + 暗角，动态流动极光 |
| 💡 **霓虹标题** | "ComfyUI 创作画室" 渐变流动 + 紫色呼吸发光 |
| 🎨 **高饱和配色** | 主色饱和度从 0.22→0.28，强调色 0.25→0.30，更鲜艳的赛博魔术风格 |
| 🔲 **卡片渐变顶边** | 所有卡片顶部 2.5px 紫蓝渐变亮边，悬停呼吸发光 |
| ✨ **卡片扫光** | hover 时紫蓝渐变光束扫过卡片（仅桌面端） |
| 🌈 **彩虹进度条** | 生成中进度条红橙黄绿蓝紫循环流动 |
| ⚡ **按钮点击波纹** | 点击任何按钮从点击处扩散彩色涟漪 |
| 🔵 **状态脉冲** | 连接状态绿点呼吸放大发光，断连红闪 |
| 📜 **渐变滚动条** | 滚动条 thumb 紫→蓝渐变 |
| 🎯 **工作流高亮** | 选中工作流左侧紫色发光条 + 呼吸动画 |
| 🖼️ **魔导书大图** | 缩略图 48×64 → **72×96px**（面积增大 225%） |
| 📤 **上传区美化** | 虚线拖拽 dropzone 风格，悬停紫光 |
| 🎯 **节点彩色指示** | 帮助栏 emoji → 彩色发光圆点（绿/红/蓝/黄） |
| 💨 **空白状态动画** | 空内容图标浮动 + 紫色投影 |
| 🎬 **Toast 弹性动画** | 通知弹窗 spring 弹性入场，错误渐变左边框 |
| 📱 **底部导航呼吸** | 移动端底部活动指示器脉动动画 |
| 🔽 **select 统一样式** | 全局下拉框自定义箭头 + 主题色 |
| 📖 **数据源指示条** | 魔导书数据源选中项左侧渐变发光条 |
| 🔄 **预设 placeholder** | 全局输入框占位符统一主题色 |

---

## 4. 工作流配置

### 4.1 工作流文件要求

插件只能读取 **API 格式** 的工作流 JSON。如何导出：

```
ComfyUI → Load 工作流 → 右上角 "Save (API Format)" → 得到 .json 文件
```

### 4.2 工作流分类

每个工作流可以设置一个分类标签，分类决定了该工作流可以被哪些命令使用：

| 分类 | 可用命令 |
|------|---------|
| `画` | `/画`、`/执行` |
| `图生图` | `/图生图`、`/执行` |
| `图生视频` | `/图生视频`、`/执行` |
| `编辑` | `/编辑`、`/执行` |
| （无分类） | `/执行` 仅 |

在 WebUI 的工作流管理器中右键/菜单设置分类。

### 4.3 命令与工作流的关联

插件内置的分类校验逻辑：

- `/画` → 当前工作流分类必须为「画」
- `/图生图` → 当前工作流分类必须为「图生图」
- `/图生视频` → 当前工作流分类必须为「图生视频」
- `/编辑` → 当前工作流分类必须为「编辑」
- `/执行` → 使用当前工作流，不限分类

插件会记住每个群/私聊最近使用过的工作流（上下文记忆）。
**但命令不会自动搜索并切换到匹配分类的工作流**——如果当前工作流分类不匹配，会直接拒绝并提示。
请先通过 `/工作流` 命令或 WebUI 手动切换到正确的工作流。

### 4.4 节点配置详解

为了让插件正确操控工作流，需要标记关键节点。打开 ComfyUI，找到工作流中的以下节点，记下其 **节点 ID**（鼠标悬停在节点上显示的数字），然后在 WebUI 节点配置面板中设置：

#### 正面提示词节点

通常是 **CLIP Text Encode (Prompt)** 节点的 `positive` 输入。插件会将用户输入的提示词写入此节点的 `text` 字段。

#### 负面提示词节点

通常是 **CLIP Text Encode (Prompt)** 节点的 `negative` 输入。插件会写入通用负面词。

#### 分辨率节点

通常是 **Empty Latent Image** 节点。插件会覆盖其 `width` 和 `height`。

#### 图片上传节点

通常是 **Load Image** 节点。插件会在图生图/编辑时传入参考图路径。

> **不是所有节点都需要标记！** 只标记你要让插件控制的节点。例如你的工作流已经写死了分辨率，就不标记分辨率节点。

### 4.5 禁用节点组

WebUI 中可以为工作流配置「节点组」（Group）。在设置面板的「禁用组」中勾选后，对应组内的所有节点将**从工作流中移除**。适用于：

- 条件性启用/禁用 ControlNet
- 开关 LoRA 加载
- 不同质量模式之间的切换

---

## 5. 魔导书系统

### 5.1 什么是魔导书？

魔导书是插件内置的提示词标签搜索引擎，包含超过 **24000 条角色数据**和大量画师/服装/场景/光影/姿势/镜头标签。帮助用户快速找到合适的提示词标签。

### 5.2 数据来源

```
data/
├── anima/
│   ├── characters.json     24000+ 角色（按 IP、发色、瞳色、人数分类）
│   ├── artists.json        画师（按热度排序）
│   └── clothing.json       服装（按分类、颜色）
├── scene/environment.json  场景/环境
├── lighting/lighting.json  光影/色调
├── shot/framing.json       镜头/构图
├── pose_action/            动作/姿势/表情
└── custom/                 用户自定义扩展（可自行添加）
```

### 5.3 在 WebUI 中使用

1. 点击正面提示词框上方的 📖 **魔导书** 按钮
2. 底部弹出面板，选择数据维度（角色/画师/服装/场景等）
3. 输入关键词搜索，或使用筛选维度缩小范围
4. 点击标签 → 自动复制到剪贴板

### 5.4 固定标签（Pin）系统

在魔导书中可固定（📌）某个条目，固定后的标签会自动合并到每次生图的 prompt 中：

- 同一子分类只能固定一条
- 固定标签有冲突检测：如果用户 prompt 中已包含相同标签，不会重复追加
- 性别/人数冲突检测：用户写 `1girl` 时，固定标签中的 `1girl` 不追加
- 外貌冲突检测：用户写了头发/眼睛颜色时，固定标签的同类描述不追加
- 固定标签在 `/随机图` 命令中始终被包含

### 5.5 随机池（Random Pool）

将子分类加入随机池后，每次 `/随机图` 会从每个子分类中随机取一条标签：

- 每个子分类可单独加入/移出随机池（🎲 按钮）
- 已固定的子分类不参与随机抽取
- 抽取的标签全局去重，避免相同标签重复出现
- 随机池状态在顶部固定栏显示

### 5.6 LLM 魔导书管理（v3.1.6+）

魔导书启用的前提下，LLM 可以通过聊天直接操作魔导书：

| LLM 工具 | 功能 |
|----------|------|
| `comfyui_search_tags` | 搜索标签（不传 source 搜全部，传 source 搜单个数据源） |
| `comfyui_list_grimoire` | 列出当前随机池和固定标签 |
| `comfyui_manage_pool` | 添加/移除数据源到随机池（action: add/remove） |
| `comfyui_manage_pin` | 固定/取消固定标签（action: pin/unpin） |

> 魔导书开关关闭后，LLM 只能搜索 Anima 旧库，所有修改工具都会拒绝操作。

### 5.7 自定义扩展数据

在 `data/custom/` 目录下创建 `.json` 文件，格式如下：

```json
[
  {
    "name": "显示名称",
    "tags": "逗号分隔的提示词标签",
    "category": "分类名（可选）",
    "note": "备注（可选）"
  }
]
```

重启插件即可生效。

---

## 6. LLM 工具调用

### 6.1 已注册工具

插件向 LLM 注册了 **16 个**可调用工具（Function Calling），供大模型自动调用：

**生图类（FunctionTool）：**

| 工具名 | 功能 | 参数 |
|--------|------|------|
| `comfyui_draw` | 文生图 | `prompt`, `ratio`（可选） |
| `comfyui_img2img` | 图生图 | `prompt`, `image_url`, `denoise`（可选） |
| `comfyui_video` | 图生视频 | `image_url`, `prompt`（可选） |
| `comfyui_edit` | 编辑图片 | `prompt`, `image_urls`（最多3张） |
| `comfyui_random` | 随机抽卡 | `count` |
| `comfyui_random_image` | 魔导书随机图 | `count` |
| `comfyui_switch_workflow` | 切换工作流 | `keyword` |
| `comfyui_list_workflows` | 列出工作流 | 无参数 |
| `comfyui_get_current_workflow` | 查看当前工作流 | 无参数 |
| `comfyui_queue_status` | 查看队列 | 无参数 |
| `comfyui_stop` | 停止生成 | 无参数 |
| `comfyui_execute` | 执行工作流 | `prompt` |

**魔导书管理类（`@filter.llm_tool`）：**

| 工具名 | 功能 | 参数 |
|--------|------|------|
| `comfyui_search_tags` | 搜索标签 | `keyword`, `source`（可选，指定数据源） |
| `comfyui_list_grimoire` | 列出随机池/固定标签 | 无参数 |
| `comfyui_manage_pool` | 管理随机池 | `action`（add/remove）, `source` |
| `comfyui_manage_pin` | 管理固定标签 | `action`（pin/unpin）, `source`, `name`, `tags` |

### 6.2 图片处理机制（重要）

当用户通过消息发送图片时（如引用图片、上传图片），插件会自动拦截 LLM 请求：

```
用户发送图片
    ↓
@filter.on_llm_request() 拦截
    ↓
图片下载到本地临时目录（base64 → PNG / HTTP → 下载）
    ↓
以文本形式注入 prompt：
  "[用户发送了图片，已保存到: /path/to/image.png]"
    ↓
LLM 使用此路径调用 img2img / video / edit 等工具
    ↓
图片上传到 ComfyUI 后，临时文件即时删除
    ↓
定时清理兜底：每 1 小时清理超过 24 小时的残留文件
```

**适用场景**：纯文本模型（如 DeepSeek V4 Flash）不支持 `image_url` 消息类型时，此机制确保 LLM 仍能感知用户发图并调用对应工具。

### 6.3 工具调用示例

用户在 QQ 中说："帮我把这张照片转成动漫风格"

```
LLM 内部流程：
1. 感知到用户引用了图片
2. 插件将图片保存为本地路径
3. LLM 调用 comfyui_img2img(prompt="转成动漫风格", image_url="...路径...")
4. 插件提交 ComfyUI → 下载结果 → 发送到群
```

---

## 7. 配置项说明

### 7.1 配置文件位置

所有配置存储在插件目录下的 `plugin_config.json`。**建议通过 WebUI 修改**，也可直接编辑。

### 7.2 配置项一览

| 配置键 | 默认值 | 说明 |
|--------|--------|------|
| `comfyui_url` | `127.0.0.1:8188` | ComfyUI 服务地址 |
| `workflow_dir` | — | 工作流 JSON 存放目录 |
| `output_dir` | — | 生成图片保存目录 |
| `comfyui_input_dir` | — | ComfyUI input 目录（本地模式上传用） |
| `upload_mode` | `local` | 上传模式：`local` / `remote` |
| `webui_port` | `8898` | WebUI 端口 |
| `webui_lan` | `false` | 是否允许局域网访问 |
| `webui_ipv6` | `false` | 是否允许 IPv6 访问 |
| `default_quality` | `720p` | 默认质量等级 |
| `default_ratio` | `9:16` | 默认图片比例 |
| `current_quality` | `720p` | 当前质量等级 |
| `current_ratio` | `9:16` | 当前比例 |
| `current_width` | — | 当前宽度（自动计算） |
| `current_height` | — | 当前高度（自动计算） |
| `prompt_log_days` | `3` | 引用图片查询提示词的记录保留天数 |
| `show_prompt_on_image` | `false` | 图片消息末尾是否自动附带提示词 |

### 7.3 质量预设与分辨率计算

```
总像素数 = 质量预设的像素值（例如 720p = 921,600）
width × height = 总像素数
width / height = 比例（例如 9:16）
```

示例：720p + 9:16 → `720 × 1280`（横向像素较少，纵向较多）

### 7.4 上传模式

| 模式 | 说明 | 适用场景 |
|------|------|---------|
| `local` | 图片写入 ComfyUI 的 input 目录 | ComfyUI 和插件在同一台机器 |
| `remote` | 图片通过 HTTP POST 上传到 ComfyUI `/upload` 端点 | ComfyUI 在远程服务器 |

---

## 8. 常见问题

### Q1: WebUI 卡顿/掉帧

这是 `backdrop-filter: blur()` 玻璃态效果的已知问题。

**解决方案**：
- ✅ 推荐使用 **Firefox** 浏览器（全速运行）
- ⚠️ Edge/Chrome 可能因 GPU 黑名单降级为 CPU 渲染导致卡顿

### Q2: 生成失败/超时

排查步骤：

1. 确认 ComfyUI 正在运行 → 浏览器打开 `http://127.0.0.1:8188`
2. 确认 ComfyUI 地址配置正确 → WebUI 设置面板检查
3. 确认 ComfyUI 已加载所需模型（Checkpoint、LoRA、ControlNet 等）
4. 检查 AstrBot 日志中的具体报错信息

### Q3: 工作流提交报错 "节点未找到"

- 确认工作流文件是 **API 格式** 导出的（非普通 Save 格式）
- 确认节点 ID 配置正确（WebUI 节点配置中标记的 ID 与工作流 JSON 一致）
- 如果工作流更新了，需要重新标记节点

### Q4: 撤回功能不可用

撤回依赖 OneBot API 返回的 `message_id`：
- **QQ 平台 (OneBot)**：完整支持
- **Telegram / Discord / 飞书**：不支持撤回，其余功能正常

### Q5: 多个用户同时使用

- 插件支持多用户并发
- 每个用户的任务独立排队（`_generating_locks[user_id]`）
- `/停止` 仅停止当前用户自己的任务

### Q6: 图片上传失败

- **本地模式**：检查 ComfyUI input 目录是否存在且有写入权限
- **远程模式**：检查 ComfyUI 的 `/upload` 端点是否可访问

### Q7: 画廊不显示图片

- 检查「输出目录」配置是否正确
- 只有插件生成的图片才会显示在画廊中
- 画廊最多扫描 5000 张图片（防止 OOM）

### Q8: 浏览器缓存导致 WebUI 不更新

修改 `webui.html` 后，按 `Ctrl+F5` 硬刷新清除缓存。部分浏览器需要多次刷新。

---

## 🔧 开发者速查

### 测试

```bash
# 启动测试服务器 + 运行全部测试
python test_plugin.py --start

# 测试已运行的服务器
python test_plugin.py --url http://127.0.0.1:8898
```

测试覆盖 27 项：服务器启动、HTML 结构、JS 语法、图标系统、API 路由、CSS 变量、边界情况。

### 端口一览

| 服务 | 端口 |
|------|------|
| WebUI 面板 | 8898 |
| ComfyUI | 8188 |
| AstrBot 面板 | 6185 |
| 测试服务器 | 19840 |

---

## 🔄 备份指南

更新插件版本或迁移到新电脑时，请备份以下文件来保留所有个人配置和数据：

### 必须备份（保留全部个人数据）

| 文件/目录 | 路径 | 说明 |
|-----------|------|------|
| `data/user/` 目录 | `...astrbot_plugin_comfyui_local/data/user/` | **用户数据统一目录**。包含以下全部用户个人配置： |
| ├ `config.json` | `...data/user/config.json` | 所有 WebUI 设置：工作流节点参数、Lora/模型保存值、魔导书固定标签、随机池、工作流绑定、分类排序、自定义背景等 |
| ├ `prompt_log.json` | `...data/user/prompt_log.json` | 历史提示词记录（用于 `/提示词` 命令） |
| ├ `grimoire/` | `...data/user/grimoire/` | **魔导书用户自定义数据源**：用户添加的角色IP、场景、光影、姿势等所有自定义JSON文件 |
| ├ └ `anima/xxx.json` | 示例：`...data/user/grimoire/anima/角色lora.json` | 用户通过 WebUI 新增的角色数据 |
| ├ └ `lighting/xxx.json` | 示例：`...data/user/grimoire/lighting/光影.json` | 用户新增的光影预设 |
| └ `cache/` | `...data/user/cache/` | Anima 角色图片缓存（可选，可重新下载） |

> 💡 **一键备份**：只需备份 `data/user/` 整个文件夹，即可保留全部个人数据。换电脑或更新插件时，将其复制到新环境的 `data/user/` 目录即可。
>
> **新增自定义魔导书数据**：在 WebUI 魔导书面板 → 编辑模式 → 点击总分类旁的 ➕ 按钮，新建的子分类自动保存在 `data/user/grimoire/` 下，一键备份时自然包含。
> 
> ⚠️ `data/anima_tools/` 目录是插件自带的 8000+ 角色数据，**无需备份**，更新插件时会自动更新。

### 建议备份（方便恢复）

| 文件 | 说明 |
|------|------|
| `.env` | AStrBot 环境变量（如果有） |
| `metadata.yaml` | 插件元数据（通常无需改动） |

### 更新插件步骤

1. **备份** `plugin_config.json` 和 `data/` 目录
2. 替换新版本文件（保留 `plugin_config.json` 和 `data/` 不覆盖）
3. 重启 AStrBot
4. 登录 WebUI 检查配置是否完整

> ⚠️ 注意：`custom_bg`（自定义背景图片）现在已保存到后端 `plugin_config.json` 中，不会因为浏览器强刷或清空 localStorage 而丢失。如需清除，在 WebUI 设置中点击「重置背景」或「重置所有用户设定」即可。

---

**作者**：lxmsqm, 黑谷  
**仓库**：https://github.com/lxmsqm/astrbot-omnipotent-comfyui  
**许可证**：MIT
